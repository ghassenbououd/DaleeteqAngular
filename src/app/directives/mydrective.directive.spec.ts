import { MydrectiveDirective } from './mydrective.directive';

describe('MydrectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MydrectiveDirective();
    expect(directive).toBeTruthy();
  });
});
